name: Test-Code

on:
  pull_request:
     branches: master 

jobs:
  test:

    runs-on: ubuntu-latest

    steps: 
    - uses: actions/checkout@v1
    - name: Run python test
      run: |
        echo $GITHUB_REF
        ls
        python test_basecode.py
